/// <reference types="cypress"/>

import { validateInput } from "../lib/functions/05_validateInputs";

describe('Validar busca pelo mapa', () => {
   it("05 - Validar busca pelo mapa", () => {
      validateInput()
   })
})
